[project]
name = "campus-map-mcp"
version = "1.0.0"
description = "中南民族大学校园百事通 MCP Server - 提供校园设施查询、导航、定位等服务"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
]

[tool.setuptools]
py-modules = ["server"]
